import { dashboardNavItems, advisoryQuickLinks } from './dashboardNavConfig';

/** Same as base config — metrics and order live in dashboardNavItems */
export const dashboardNavItemsModern = dashboardNavItems;

export { advisoryQuickLinks };
